# ================================================
# Configuração de PRODUÇÃO para o backend
# ================================================
# Copie este arquivo para .env e edite os valores:
#   cp .env.production.example .env
#   nano .env
# ================================================

PORT=3001
HOST=0.0.0.0
NODE_ENV=production

# OBRIGATÓRIO: troque por uma chave aleatória forte.
# Gere com:  node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=TROQUE_AQUI_POR_UMA_CHAVE_ALEATORIA_FORTE

# Sessão persistente. Exemplos: 30d, 90d, 365d
JWT_EXPIRES_IN=30d

# OAuth 2.0 para gateway de integracao. Nunca reutilize JWT_SECRET.
# Gere um valor forte com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SERVICE_JWT_SECRET=TROQUE_AQUI_POR_OUTRA_CHAVE_ALEATORIA_FORTE
SERVICE_TOKEN_ISSUER=https://api.seu-dominio.com
SERVICE_TOKEN_AUDIENCE=vetor-gateway
SERVICE_TOKEN_EXPIRES_IN=1h

# PostgreSQL
DB_HOST=postgres
DB_PORT=5432
DB_NAME=gestao_obras
DB_USER=gestao_user
DB_PASSWORD=TROQUE_AQUI_POR_UMA_SENHA_FORTE
DB_POOL_MAX=20
DB_POOL_IDLE_TIMEOUT=30000
DB_POOL_CONNECTION_TIMEOUT=5000
